# Grafana Session Table Initializer

Dockerfiles to help initialize `session` table in database backends.

Currently only works for provider `mysql`.

## MySQL Guide

Check the `README.md` in [`mysql`](./mysql) for more information.
